# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

# Prague Trip App

אפליקציית תכנון טיול לפראג, 21–26.10.2026 (6 ימים) — מימוש ראשון של פרויקט רחב יותר: אתר במסך מלא ואפליקציה מותאמת לכל סוגי הטלפונים הניידים, לתכנון טיולים של 10 ימים בחו"ל למשפחות עם ילדים.

## 🔴 חוק ברזל: עברית ו-RTL

**כל קוד, אתר, אפליקציה או מסמך שנוצרים בפרויקט חייבים לתמוך באופן מלא בשפה העברית וביישור מימין לשמאל (RTL).** זהו כלל שאינו ניתן לפשרה — לא רק ברמת טקסט תצוגה, אלא בכל היבט: מבנה HTML (`dir="rtl"`, `lang="he"`), פריסת CSS/Tailwind (שימוש בתכונות לוגיות ולא ב-`left`/`right` קשיחים כשמשמעותם כיוונית), אייקונים וחצים שמשקפים כיוון RTL, פורמט תאריכים/מספרים, טפסים, הודעות שגיאה, ותיעוד. כל תוכן, קומפוננטה, מסמך README או הנחיה חדשה שנוספים לפרויקט — כתובים בעברית ונבדקים ב-RTL לפני שנחשבים גמורים.

## פקודות

```bash
npm run dev      # שרת פיתוח (localhost:3000)
npm run build    # build לפרודקשן
npm run start    # הרצת ה-build
npm run lint     # ESLint (eslint-config-next: core-web-vitals + typescript)
```

אין כרגע suite של טסטים בפרויקט.

## עקרונות פרויקט

- **מטרה**: אתר במסך מלא + אפליקציה מותאמת (responsive) לכל סוגי הטלפונים הניידים, לתכנון טיולים של 10 ימים בחו"ל למשפחות עם ילדים. הפרויקט הנוכחי (טיול פראג, 6 ימים) הוא המימוש הראשון של הרעיון — יש לבנות ולהרחיב אותו כך שיתאים גם לטיולים ארוכים יותר ולתוכן ממוקד-משפחה (התאמה לילדים, קצב, נגישות).
- **Stack**: Next.js 16 (App Router) + React 19 + TypeScript + Tailwind CSS v4.
- **Mobile-first**: כל מסך מעוצב קודם לטלפון, ואז מורחב למסכים גדולים ולמסך מלא.
- **RTL/עברית**: ראה "חוק ברזל" למעלה — `dir="rtl"` ו-`lang="he"` ברמת המסמך (`src/app/layout.tsx`), וכל טקסט חדש בעברית.
- **ללא Database**: כל הנתונים סטטיים בקוד, בלי שכבת נתונים חיצונית.
- **ללא שירותים בתשלום**: אין להשתמש ב-API keys, שירותי מפות/מזג אוויר בתשלום וכו'.
- **הפרדת נתונים מ-UI**: כל תוכן הטיול (ימים, אטרקציות, לו"ז) חי ב-`src/data/prague-trip.ts` בלבד. קומפוננטות אחראיות רק להצגה.
- **מפות**: קישורי Google Maps רגילים דרך `googleMapsSearchUrl()` ב-`src/lib/maps.ts` (חיפוש/קואורדינטות), לא Maps API בתשלום.
- **דיוק עובדתי**: אסור להמציא מידע חסר (שעות פתיחה, מחירים, כתובות מדויקות, מזג אוויר, זמני הליכה). מידע לא ידוע מיוצג כ-placeholder מפורש בטיפוסים ובתצוגה — ראה "מודל הנתונים" למטה.
- **עיצוב**: נקי, מודרני, נוח מאוד לשימוש ביד אחת בטלפון.

## ארכיטקטורה

**ניתוב**: שתי דפי-מסלול בלבד תחת `src/app` — `/` (רשימת 6 ימי הטיול) ו-`/day/[dayId]` (פירוט יום בודד, `generateStaticParams` על כל 6 הימים). כל התוכן של דף יום נשלף בעזרת `getTripDay(id)` מתוך `src/data/prague-trip.ts` ומועבר כ-props לרצף קומפוננטות ב-`src/components` (Timeline, AttractionCard, TravelInfo, MealsSection, WeatherCard וכו').

**מודל הנתונים היחיד**: `src/data/prague-trip.ts` הוא מקור האמת היחיד — טיפוסים (`TripDay`, `Attraction`, `TimelineItemData`, `Meal`, `TravelSegment`...) וגם הנתונים עצמם (`day1`, ...`tripDays`). ימים שטרם תוכננו נוצרים עם `emptyDay(id, date)`. קומפוננטות לא ניגשות לנתונים גולמיים — רק דרך הטיפוסים והפונקציות המיוצאות מהקובץ הזה.

**דפוס `Fact<T>`**: כדי למנוע המצאת מידע חסר, כל שדה שעלול להיות לא ידוע (מזג אוויר, אזור מרכזי, תוכנית גשם, זמני הליכה) מטופל כ-`Fact<T>` — union של `{ status: "known"; value: T }` או `{ status: "placeholder"; note?: string }`, שנבנה עם הפונקציות `known()` / `placeholder()`. `Price` הוא union דומה עם שלושה מצבים: `verified` / `estimated` / `placeholder`. כל קומפוננטה שמציגה שדה כזה (למשל `WeatherCard`, `PriceTag`) בודקת קודם את ה-`status` ומרנדרת מצב placeholder ייעודי (בדרך כלל מסגרת מקווקוות + טקסט מסביר) לפני שהיא ניגשת ל-`value`. בעת הוספת שדה עובדתי חדש שעשוי להיות חסר — עטוף אותו ב-`Fact<T>` ולא תשים ערך בדוי.

**תמונות**: `AttractionImage` כולל `src` (תחת `public/images/day{N}/...`), `alt`, ו-`credit`/`sourceUrl`/`license` לייחוס. כל תמונה שמתווספת חייבת לכלול ייחוס ורישיון תקין (למשל תמונות Wikimedia Commons בשימוש כרגע).
